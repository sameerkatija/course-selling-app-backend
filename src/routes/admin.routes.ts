import { Router } from "express";

const router = Router();

export default router;

// /admin/dashboard

// /admin/users

// /admin/approve-teacher

// /admin/courses

// /admin/analytics (optional)
